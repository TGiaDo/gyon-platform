{
  "name": "gyon-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Open-source navigation platform.",
  "license": "MIT",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.10.8",
    "typescript": "^7.0.2"
  }
}
